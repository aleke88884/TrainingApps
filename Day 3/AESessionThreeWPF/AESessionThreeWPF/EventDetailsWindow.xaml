<Window x:Class="AESessionThreeWPF.EventDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AESessionThreeWPF"
        xmlns:chartingToolkit="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=DotNetProjects.DataVisualization.Toolkit"

        mc:Ignorable="d"
        Title="Event Details" WindowStartupLocation="CenterScreen" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBlock x:Name="EventNameTxt" Margin="5" Text="Event"/>
            <TextBlock x:Name="CostTxt" Margin="100,5,0,5" Text="Cost:"/>
            <TextBlock x:Name="ProfitLostTxt" Margin="100,5,0,5" Text="Profit/Loss:"/>
            <TextBlock x:Name="SoldTicketsTxt" Margin="100,5,0,5" Text="Sold tickets:"/>
            <TextBlock x:Name="AvailableTicketsTxt" Margin="100,5,0,5" Text="Available tickets:"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <TextBlock Text="Tiers" FontWeight="Bold" VerticalAlignment="Center" Margin="5,0" />

            <Border BorderBrush="Gray" CornerRadius="12" BorderThickness="1" Margin="2" VerticalAlignment="Center" Width="80" Height="28" HorizontalAlignment="Right" Background="White">
                <Button x:Name="BtnOptimize" 
            Content="Optimize" 
            BorderBrush="Transparent" 
            Background="Transparent"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Padding="0"
            Margin="0"/>
            </Border>


        </Grid>
        <DataGrid Grid.Row="2" Margin="5" x:Name="TiersDataGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tier Name" Binding="{Binding name}" Width="*"/>
                <DataGridTextColumn Header="Saleability" Binding="{Binding name}" Width="*"/>
                <DataGridTextColumn Header="Demand" Binding="{Binding name}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding name}" Width="*"/>
                <DataGridTextColumn Header="Base Price" Binding="{Binding name}" Width="*"/>
                <DataGridTextColumn Header="Available" Binding="{Binding name}" Width="*"/>
                <DataGridTextColumn Header="Max" Binding="{Binding name}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" >
            <TextBlock Text="Forecast" Margin="5" FontWeight="Black"/>
            <Border Width="100" VerticalAlignment="Center" Height="25" BorderThickness="1" Margin="150,3" BorderBrush="Gray" CornerRadius="12">
                <Button x:Name="BtnExport"  Content="Export" BorderBrush="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Background="Transparent"/>    
            </Border>
            
        </StackPanel>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DataGrid x:Name="TierForecastDataGrid" Grid.Column="0" Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tier Name" Binding="{Binding name}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding name}" Width="*"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding name}" Width="*"/>
                    <DataGridTextColumn Header="Saleability" Binding="{Binding name}" Width="*"/>
                    <DataGridTextColumn Header="Is forecast" Binding="{Binding name}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <chartingToolkit:Chart Grid.Column="1" Margin="5" Title="Line Chart">
                <chartingToolkit:LineSeries Title="Sales Over Time" >
                    
                </chartingToolkit:LineSeries>
            </chartingToolkit:Chart>
        </Grid>
    </Grid>
</Window>
